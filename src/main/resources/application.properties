spring.datasource.url=jdbc:postgresql://172.104.186.148:5432/library
spring.datasource.username=ypathan
spring.datasource.password=nokian876
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.sql.init.mode=always

# Logging configuration
logging.level.org.springframework=info
logging.level.com.StudentLibrary=debug
logging.level.org.springframework.security=TRACE

# Book configuration
books.max_allowed=5
books.max_allowed_days=10
books.fine.per_day=5

# Server configuration
server.port=8080
spring.application.name=student-library

# File upload configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Hibernate configuration for large objects
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true


